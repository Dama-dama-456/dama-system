# ============================================================
# إعدادات نظام داما القابضة
# قم بتغيير القيم أدناه قبل تشغيل النظام
# ============================================================

# كلمة مرور قاعدة البيانات (غيّرها إلى كلمة مرور قوية)
DB_PASSWORD=your_password_here

# مفتاح تشفير الجلسات (رمز عشوائي — لا تشاركه مع أحد)
SESSION_SECRET=DamaHolding_SecretKey_ChangeThis_2025

# منفذ الوصول للموقع (80 = بدون رقم في الرابط)
APP_PORT=80
